# LocalCast

## Что делает LocalCast

LocalCast - это локальный web-сервис на Python для управления и трансляции мультимедийного контента внутри локальной сети.

Сервис позволяет:

- создавать отдельные папки трансляций для разных телевизоров;
- загружать видео и изображения через административную панель;
- автоматически воспроизводить контент в browser player;
- управлять воспроизведением через web-интерфейс;
- использовать систему на Smart TV, телефонах, планшетах и компьютерах.

---

# Основные возможности

- авторизация администратора;
- смена логина и пароля через web-интерфейс;
- создание папок трансляций;
- загрузка одного или нескольких файлов;
- удаление отдельных файлов;
- удаление всех файлов;
- отображение списка загруженного контента;
- browser-based player;
- автоматическое воспроизведение медиа;
- поддержка видео и изображений;
- repeat playlist;
- repeat one;
- shuffle режим;
- переключение между файлами;
- fullscreen режим;
- управление с клавиатуры;
- уведомления об успешных действиях.

---

# Поддерживаемые форматы

## Видео

- `.mp4`
- `.mkv`
- `.avi`
- `.mov`
- `.webm`
- `.m4v`

## Изображения

- `.jpg`
- `.jpeg`
- `.png`
- `.webp`

---

# Как работает система

1. Сервер запускается на компьютере внутри локальной сети.
2. Администратор входит в web-панель.
3. Через админку создаются папки трансляций.
4. В каждую папку загружается собственный контент.
5. Пользователь открывает browser player по ссылке `/player/{id}`.
6. Контент автоматически воспроизводится по очереди.

---

# Важно знать

- сервер и устройства должны находиться в одной локальной сети;
- контент управляется через административную панель;
- Smart TV открывает трансляцию через встроенный браузер;
- воспроизведение работает без VLC и сторонних приложений;
- каждая папка трансляции имеет собственный browser player.

---

# Структура проекта

```text
LocalCast/
│
├── server.py
├── config.py
├── media_lib.py
│
├── data/
│   ├── credentials.json
│   └── folders.json
│
├── templates/
│   ├── admin.html
│   ├── folder.html
│   ├── player.html
│   └── login.html
│
├── tv_content/
│   ├── common/
│   ├── 1/
│   └── 2/
│
└── README.md
```

---

# Установка зависимостей

```bash
pip install fastapi uvicorn jinja2 python-multipart itsdangerous
```

---

# Запуск сервера

```bash
python server.py
```

После запуска сервер будет доступен локально по адресу:

```text
http://127.0.0.1:8000
```

Для подключения с других устройств внутри локальной сети необходимо использовать IP-адрес компьютера:

```text
http://IP_КОМПЬЮТЕРА:8000
```

---

# Проверка работы сервера

```text
http://127.0.0.1:8000/health
```

Если всё работает, вернётся:

```json
{"status":"ok"}
```

---

# Как узнать IP компьютера

## macOS

```bash
ipconfig getifaddr en0
```

Если используется LAN-подключение:

```bash
ipconfig getifaddr en1
```

Также можно использовать:

```bash
ifconfig
```

---

## Windows

```bash
ipconfig
```

Нужен параметр:

```text
IPv4 Address
```

---

# Авторизация администратора

Перед входом в административную панель необходимо авторизоваться.

Страница входа:

```text
http://IP_КОМПЬЮТЕРА:8000/login
```

Учётные данные администратора хранятся в:

```text
data/credentials.json
```

Пример:

```json
{
  "username": "admin",
  "password": "123123"
}
```

После авторизации создаётся session cookie.

---

# Административная панель

Админка доступна по адресу:

```text
http://IP_КОМПЬЮТЕРА:8000/admin
```

Через административную панель доступны:

1. Создание папок трансляций;
2. Загрузка файлов;
3. Удаление отдельных файлов;
4. Удаление всех файлов;
5. Просмотр списка контента;
6. Переход к трансляции;
7. Смена логина и пароля;
8. Выход из аккаунта.

---

# Папки трансляций

Каждая папка имеет собственный browser player.

Пример:

```text
/player/common
/player/1
/player/2
```

Это позволяет разным телевизорам воспроизводить разный контент одновременно.

---

# Browser Player

Плеер поддерживает:

- воспроизведение видео;
- отображение изображений;
- autoplay;
- fullscreen режим;
- repeat playlist;
- repeat one;
- shuffle;
- переключение между файлами;
- управление с клавиатуры.

---

# Горячие клавиши

```text
Space       пауза / воспроизведение
ArrowRight  следующий файл
ArrowLeft   предыдущий файл
F           fullscreen режим
```

---

# Использование на Smart TV

Для использования на Smart TV:

1. Подключить телевизор к той же сети, что и сервер;
2. Открыть встроенный браузер;
3. Перейти по адресу:

```text
http://IP_КОМПЬЮТЕРА:8000/player/1
```

После этого трансляция начнёт воспроизводиться автоматически.

---

# Где хранятся файлы

Все медиафайлы сохраняются в:

```text
tv_content/
```

Пример:

```text
tv_content/
├── common/
├── 1/
└── 2/
```

---

# Файлы конфигурации

## credentials.json

Хранит логин и пароль администратора.

```json
{
  "username": "admin",
  "password": "123123"
}
```

---

## folders.json

Хранит отображаемые названия папок трансляций.

Пример:

```json
{
  "common": "Общий контент",
  "1": "1 этаж телевизор"
}
```

---

# Особенности реализации

Проект состоит из нескольких основных частей:

- `server.py` - FastAPI backend;
- `config.py` - настройки проекта;
- `media_lib.py` - работа с медиаконтентом и папками;
- `templates/admin.html` - административная панель;
- `templates/folder.html` - управление контентом папки;
- `templates/player.html` - browser player;
- `templates/login.html` - авторизация администратора.

---

# Безопасность

В проекте реализована session-based авторизация администратора.

Защищены:

- вход в админку;
- создание папок;
- загрузка файлов;
- удаление файлов;
- удаление папок;
- смена учётных данных.

Также реализована защита от выхода за пределы папок при запросе файлов через URL.

---

# Назначение проекта

LocalCast может использоваться как локальная система управления мультимедийным контентом для:

- Smart TV;
- учебных аудиторий;
- офисов;
- магазинов;
- рекламных экранов;
- digital signage систем;
- локальных презентационных стендов.

---

# Пример сценария использования

1. Администратор запускает сервер;
2. Выполняет вход в `/login`;
3. Создаёт папку трансляции;
4. Загружает контент через `/admin`;
5. Открывает `/player/{id}` на телевизоре;
6. Контент автоматически воспроизводится;
7. Разные телевизоры могут показывать разный контент одновременно.